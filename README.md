# Dezpila Clone

Plataforma de streaming "Dezpila", um clone de serviço de streaming de vídeo moderno e responsivo.

## 🚀 Tecnologias Utilizadas

Este projeto foi desenvolvido com uma stack moderna e robusta:

*   **Frontend:** React 19, TypeScript, Vite
*   **Estilização:** Tailwind CSS (v4), Shadcn/UI, Radix UI, Framer Motion
*   **Backend:** Express (Node.js)
*   **Build:** Esbuild
*   **Gerenciamento de Pacotes:** npm / pnpm

## 📦 Instalação e Execução

Siga os passos abaixo para rodar o projeto localmente:

1.  **Clone o repositório:**
    ```bash
    git clone https://github.com/SEU_USUARIO/dezpila_clone.git
    cd dezpila_clone
    ```

2.  **Instale as dependências:**
    ```bash
    npm install --legacy-peer-deps
    ```
    > **Nota:** Use a flag `--legacy-peer-deps` devido a conflitos de versão com alguns plugins do Vite.

3.  **Inicie o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```
    A aplicação estará disponível em `http://localhost:5173` (ou porta similar).

## 🛠️ Scripts Disponíveis

*   `npm run dev`: Inicia o servidor de desenvolvimento Vite.
*   `npm run build`: Compila o frontend com Vite e o backend com Esbuild para produção.
*   `npm run start`: Inicia a aplicação em modo de produção (exige build prévio).
*   `npm run check`: Verifica tipos do TypeScript.
*   `npm run format`: Formata o código usando Prettier.

## 📱 Funcionalidades

*   Interface imersiva e responsiva (Mobile first).
*   Catálogo de filmes e séries.
*   Mockup de múltiplos dispositivos na Hero Section.
*   Player de vídeo integrado (simulação).

## 👤 Autor

**Osmar Francisco Cavalcante**
[osmar@osmardev.online](mailto:osmar@osmardev.online)